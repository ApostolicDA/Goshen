goshen:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: goshen-analytics
      dataset: analytics
      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}"
      location: US
      threads: 4
      timeout_seconds: 300